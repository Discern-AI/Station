# Local development

1. Copy `.env.example` to `.env`
2. Install with `pnpm install`
3. Run `pnpm dev`
4. Web defaults to http://localhost:3000
5. API defaults to http://localhost:4000

Studio Alpha uses in-memory mock repositories until the real database layer is wired.
